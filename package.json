{
  "name": "my-obsidian-plugins",
  "private": true,
  "version": "0.1.1",
  "workspaces": ["plugins/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "esbuild": "^0.28.2",
    "obsidian": "^1.8.7",
    "typescript": "^5.9.2",
    "vitest": "^5.0.1"
  }
}
